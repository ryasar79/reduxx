'use strict';


module.exports = Object.freeze(

    function( mainComponent ) {

        const {

            reduxXCore,

        } = this;

        reduxXCore.mainComponent = mainComponent;
    }
);
